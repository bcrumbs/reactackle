'use strict';

import styled from 'styled-components';
import { baseModule } from '../../../../theme/styleHelpers';

export const PropsDescriptionStyled = styled.span`
  margin-top: ${baseModule(1)}px;
`;

PropsDescriptionStyled.displayName = 'PropsDescriptionStyled';
