'use strict';

import styled from 'styled-components';

import {
  fontColorMedium,
} from '../../../../theme/styleHelpers';

export const PropsDefaultStyled = styled.div`
  color: ${fontColorMedium};
`;

PropsDefaultStyled.displayName = 'PropsDefaultStyled';
