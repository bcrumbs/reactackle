'use strict';

import styled from 'styled-components';

const propTypes = {};
const defaultProps = {};

export const TooltipContentStyled = styled.div`
  color: inherit;
  font-size: inherit;
  line-height: inherit;
`;

TooltipContentStyled.propTypes = propTypes;
TooltipContentStyled.defaultProps = defaultProps;
TooltipContentStyled.displayName = 'TooltipContentStyled';
