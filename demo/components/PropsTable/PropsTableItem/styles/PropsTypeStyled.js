'use strict';

import styled from 'styled-components';
import {
  fontWeightNormal,
} from '../../../../theme/styleHelpers';

export const PropsTypeStyled = styled.span`
  font-weight: ${fontWeightNormal};
`;

PropsTypeStyled.displayName = 'PropsTypeStyled';
