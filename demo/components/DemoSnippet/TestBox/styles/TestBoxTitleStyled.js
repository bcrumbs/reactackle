'use strict';

import styled from 'styled-components';
import { baseModule } from '../../../../theme/styleHelpers';

export const TestBoxTitleStyled = styled.div`
  padding: ${baseModule(1)}px ${baseModule(2)}px ${baseModule(1)}px 0;
`;

TestBoxTitleStyled.displayName = 'TestBoxTitleStyled';
