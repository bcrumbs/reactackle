'use strict';

import styled from 'styled-components';

const propTypes = {};
const defaultProps = {};

export const CardAreaSideStyled = styled.div`
  display: flex;
  align-items: stretch;
`;

CardAreaSideStyled.propTypes = propTypes;
CardAreaSideStyled.defaultProps = defaultProps;
CardAreaSideStyled.displayName = 'CardAreaSideStyled';
