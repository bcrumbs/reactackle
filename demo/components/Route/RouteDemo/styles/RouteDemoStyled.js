'use strict';

import styled from 'styled-components';
import { baseModule } from '../../../../theme/styleHelpers';

export const RouteDemoStyled = styled.div`
  padding: ${baseModule(4)}px 0;
  width: 100%;
`;

RouteDemoStyled.displayName = 'RouteDemoStyled';
